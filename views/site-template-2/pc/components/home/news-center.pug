.new-center.clearfix
	mixin news-box(title,newsList)
		.news-box
			.title
				.line
				h3 #{ title }
				a(href= newsLink)
					img(src="//file.baixing.net/202102/07403ddfb02bc55c442329926b7e0c2f.png", alt="更多")
				//-这里需要点击跳转
			.content
				if newsList && newsList.length > 0
					each item in newsList.length > 3 ? newsList.slice(0, 3) : newsList
						//-这里需要后端给文章的图片和内容
						a(href=item.urlSuffix)
							.left-img
								- dateObj = new Date(item.modifiedTime * 1000)
								- date = dateObj.getDate()
								- year = dateObj.getFullYear()
								- month = dateObj.getMonth() + 1
								.news-date__day #{date > 9 ? date : `0${date}`}
								.news-date__year #{year}/#{month > 9 ? month : `0${month}`}

							//-img(src=defaultImg, alt="资讯图片")
							.item-card
								.item-title #{ item.name }
								.item-content.
									#{ item.limitText }




