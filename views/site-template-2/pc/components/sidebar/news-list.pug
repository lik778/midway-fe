//- 新闻资讯
- const { articleRecommend } = renderData
if articleRecommend && articleRecommend.length > 0
  .sidebar-item 
    .sidebar-item__title
      span 新闻资讯
    .sidebar-item__content 
      .side-news-list 
        each item in articleRecommend.slice(0, 8)
          a.news(href=item.urlSuffix) #{ item.name }大法师打发斯蒂芬
