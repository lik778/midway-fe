mixin user-defined-item-content(item,type)
  .user-defined-item-content 
    .triangle(class=type)
    .title #{ item.title }
    .line 
    .desc #{ item.content }

mixin user-defined-module(autoConfig)
  - const defaultImg = ['']
  include ./home-title.pug
  each configItem in autoConfig
    +home-title(configItem.mainModuleTitle)
      //-是否为奇数个，奇数个展示查看更多
    - let { subModuleBos } = configItem
    .user-defined-list
      each item, index in subModuleBos
        .user-defined-item
          if index % 2 === 0
            .user-defined-item-img(style =`background:url(${item.urlImg ?item.urlImg: defaultImg[index]}) no-repeat center/100%`) 
            +user-defined-item-content(item,'right')
          else 
            +user-defined-item-content(item,'left')
            .user-defined-item-img(style =`background:url(${item.urlImg ?item.urlImg: defaultImg[index]}) no-repeat center/100%`) 