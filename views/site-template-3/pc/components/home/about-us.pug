.about-us-bgc
	.about-us-content
		.about-us-text
			.title 关于我们
			.title-line
			.content
				| !{ company.about.replace(/\n/g, '<br/>') || '暂时还未上传公司简介哦...' }
			a.view-more(href=aboutLink) 查看更多
		.about-us-picture
			.pic-border
			- const video = company.about.video
			- const hasVideo = video
			if hasVideo
				.video-con
					.video-wrapper
						video(src=video.url, loop, preload, controls)
					.video-cover
						.picture(style="background: url(\""+video.cover+"\")")
			else
				.picture