//-此处写产品列表html,
//-其样式文件在assets/site-template-3/pc/components/product/product-list.styl
.product-list
  if productList && productList.length >0
    each item in productList
      - const isVideo = item.videoUrl
      a.item(href=item.urlSuffix target="_blank" data-id=item.id)
        .left
          .play-icon(class=isVideo?'active':'')
          img.img(src=item.headImg || defaultImg)
          .text-content
            .name #{item.name}
            .content.only-twoline #{item.content} 
            .price #{item.price}
        .right 查看详情
  else
    p.no-content 当前页面暂无内容，敬请期待...
