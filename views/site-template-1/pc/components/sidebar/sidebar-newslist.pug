.sidebar-newslist
    - const sidebarNewsList = renderData.articleList.result
    if sidebarNewsList && sidebarNewsList.length > 0
        ul
            //-默认最多展示10条
            each item in sidebarNewsList.length>10 ? sidebarNewsList.slice(0,10) : sidebarNewsList
                li
                    a(href="/" + renderData.shopName + item.urlSuffix) #{item.name}

        //-这里跳到/n/新闻列表页，最好是获取后端传的值.查看更多，产品要求要一直存在所以加了取值判断
        //-moreLink使用的是侧边栏查看更多定义的数据
        if renderData.navigation && moreLink
            a.news-more-btn(href="/" + renderData.shopName + moreLink) 查看更多
        else
            a.news-more-btn(href="#") 查看更多
    else
        a 暂时没有数据哦
