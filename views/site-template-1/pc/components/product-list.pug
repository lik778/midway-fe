//-这个页面还缺少分页
mixin product-list(page)
	.product-list.clearfix
		if productList && productList.length >0
			each item in productList
				a.item(href=item.urlSuffix)
					img(src=item.headImg || '//file.baixing.net/202011/722f557a62889f098f7843fd3481e22b.png' )
					p #{item.name}
					.btn 了解详情
		else
			p 当前页面暂无内容，敬请期待...

	- const moreProductArr = (renderData.navigation.filter(x => x.position == "productListPage"))
	if page == 'home'
		if renderData.navigation && moreProductArr && moreProductArr.length>0
			a.product-more-btn(href=moreProductArr[0].content) 查看更多



