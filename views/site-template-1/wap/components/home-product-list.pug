.product
    - const productList= renderData.productList.result
    .product-title.clearfix
        p 服务内容
        //-- const moreProductArr = (renderData.navigation.filter(x => x.position == "productListPage"))
        if moreProductArr && moreProductArr.length>0
            a(href=moreProductArr[0].content) 更多
                .right-icon

    .product-list.clearfix
        if productList && productList.length >0
            each item in productList.length >= 4 ? productList.slice(0,4):productList
                .item
                    img(src=item.headImg || "//file.baixing.net/202011/722f557a62889f098f7843fd3481e22b.png")
                    p #{item.name}
                    a.btn(href=item.urlSuffix) 了解详情
        else
            p 暂时还没有数据哦
