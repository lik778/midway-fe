.tab-header
    ul.tab-hidden
        li
            a.active(href=cateList[0].urlSuffix) #{cateList[0].name}
        if cateList && cateList.length>0
            each item,index in cateList.slice(1)
                li(data-tab=`tab-${index}`)
                    a(href=item.urlSuffix) #{item.name}
    //-这里其实应该是判断超过2行显示。
    if  cateList.length >= 7
        .s-open 点击展开
            img(src="//file.baixing.net/202012/41b0db79b7b9316481aa69058d3ed37b.png", alt="")
        .s-off 点击收起
            img(src="//file.baixing.net/202012/13a5e125202a182d2f48da2392404cd8.png", alt="")
//-block append scripts
//-    script.
//-        window.currentHref = "#{}"

//-block append scripts
//-	script.
//-		function s_click()

