if productList && productList.length >0
	.product-list
		each item in productList
			- const isVideo = item.videoUrl
			a.item-p.maskLayer-relative(href=item.urlSuffix target="_blank")
				if isBaiduPin === 'baidu_pinzhuan' && pinZhuan
					.maskLayer-htek
				.play-icon(class=isVideo?'active':'')
				if isVideo
					video
						source(src=isVideo loop preload controls)
				else
					.em
						img(src=item.headImg || defaultImg alt=item.infoSeoKeyWord || item.name)
				p.productListP.only-oneline(data-hover=totalTip) #{ item.name }
else
	p.no-content 当前页面暂无内容，敬请期待...
