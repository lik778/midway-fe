import React, { FC, useContext, useEffect } from 'react';
import { Redirect } from 'umi';

const AiModule = () => {
  
  return <Redirect to={'/ai-module/promote-create/ai-list'}> </Redirect>
}

export default AiModule

